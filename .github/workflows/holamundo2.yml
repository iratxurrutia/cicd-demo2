name: Primer workflow

on: push

jobs:
  validar:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: echo "Hola mundo"
      - name: Bajar codigo
        uses: actions/checkout@v5
      - name: Probar codigo
        run: python3 printholamundo.py
